#include <stdio.h>
  int main()
{
  float dinero, total, i=11;
printf ("catidad a invertir: \n");
scanf ("%f", &dinero);
total = (dinero * 0.11) + dinero; 
printf ("la suma total de dinero es: %f", total);
return 0;
}
