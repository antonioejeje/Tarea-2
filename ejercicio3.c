#include <stdio.h>
  int main()
  {
float p,     dol;
printf ("Ingresa la cantidad de doloares que deseas convetir\n");
scanf ("%f", &dol);
p = dol * 11.96;
  printf ("la cantidad en pesos es: %f", p);
  return 0;
}
